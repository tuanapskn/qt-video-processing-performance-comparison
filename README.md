# qt-video-processing-performance-comparison
A C++ Qt application that performs real-time video processing using OpenCV. Implements grayscale conversion and alpha blending with CPU (single/multi-thread) and GPU (CUDA) methods, and visualizes performance comparison using QChart.
